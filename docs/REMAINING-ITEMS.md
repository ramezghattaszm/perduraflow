# PerduraFlow — remaining items before demo (single source of truth)

> Demo ~10 days out (as of phase-6 completion). Engine (phases 0–6) is feature-complete and verified. What's left = fixes + realism + improvements + staging. Grouped by priority.

---

## A. Must-fix before demo (small, known — clears the deck)
- [ ] **Per-option narration scoping** (`NARRATION-PER-OPTION-FIX.md`) — each option card gets its OWN narration; "why the winner won" appears once, not on every card. **Fix at the shared component level** (surfaced twice — demand path + wear path). Re-verify translate-only (#5) + non-blocking (#6). **Re-checked (code, this cycle) — looks resolved but NOT closed:** the shared-component fix IS present in `WhatIfOptionSet` (used by both the board demand/wear path AND the at-risk card) — each card gets its own `<Narration mode="option" optionId={o.id}>` ([whatif-option-set.tsx:242](../packages/app/features/whatif/whatif-option-set.tsx)), and the across-options "why the winner won" renders ONCE outside the `.map` (`feasibleCount >= 2`, [whatif-option-set.tsx:299](../packages/app/features/whatif/whatif-option-set.tsx)). Left OPEN pending a runtime re-check (browser + live LLM) before closing — not assuming fixed.
- [x] **"See options" UI bug — FIXED (code-path verified).** The actual reroute-path control is wired, NOT just superseded by the two-door card (which is a separate at-risk-ORDER surface). The cockpit line-down **condition card** CTA: `onPress={() => runLineDownWhatIf(c.resourceId)}` ([board-screen.tsx:1086](../packages/app/features/scheduling/board/board-screen.tsx)) → `whatIf.mutate({changeSet:{changes:[{kind:'line_down',resourceId}]}})` (board-screen.tsx:562); the option-set renders in the global popup. The down-lane panel CTA (board-screen.tsx:737) is wired identically. Commits `52a7de1` (popup migration), `bb30c04` (single-press reachability FIXED). [live click not re-run this cycle]
- [x] **Cost-credibility glance — RESOLVED.** Reseeded historical `cost_per_unit` to a believable ~12% premium over live (`fix(scheduling)` **8281977**): Saltillo $2.05 / Press A $2.15 / Press B $2.00 / Ramos $2.15 (was 8.8/9.3/8.6/6.3; the named **$9.05** is superseded). Dry-run confirmed baseline $2.07–2.09 vs live ~$1.9 → lift ~−$0.24/unit — believable, not the old 3–5×.
- [ ] **Markdown rendering in Copilot** — confirm message bubbles render markdown (bold/lists) on web AND native. (Possibly port EQALL's formatter if standalone + cross-platform; else a standard RN+web markdown renderer.)
- [x] **Delete a draft plan — DONE.** `discardDraft` service (status→`discarded`, draft-only guard → `SCHEDULE_VERSION_NOT_DRAFT`, [scheduling.service.ts:689](../apps/api/src/modules/scheduling/scheduling.service.ts)) + `DELETE /admin/scheduling/versions/:id` (admin controller) + `scheduling.discard.spec.ts` (6 tests, passing) + a draft-only **Discard** button on the board ([board-screen.tsx:955](../packages/app/features/scheduling/board/board-screen.tsx), `useDiscardDraft`). Committed/superseded versions stay immutable. Matches the item.

## B. Conversation-capability improvements (deferred — after the engine is committed)
- [ ] **Better scenario-creation understanding** — sharpen intent recognition → change-set construction when the user asks to create a scenario (more reliable language→ChangeSet mapping).
- [~] **Screen context-awareness** — the Copilot should know the current screen AND its on-screen selections (active plant, version, selected option/resource), so "explain this" / "what if we change this" grounds against actual on-screen context, not just conversation history. **VERSION-AWARENESS DONE (Tier 1, `fix(copilot)` 64ffc8b):** `explain_lateness` now honors `screenContext.versionId` (the viewed DRAFT), so "why is X late?" follows the board, not always the committed plan — dry-run proven (same order: committed→"on track", draft→"late"). The other plan-reading Copilot tools (`evaluate_what_if`/`goal_seek`/`retrieve_baseline`) were found to **re-sequence from live global inputs**, so they already reflect the viewed draft without threading a versionId (Tier 2 deemed inert + cache-fragmenting — not built). **Remaining:** the broader selected-entity grounding ("this option/resource/operator" deixis beyond version) if/when needed.
- [ ] **evaluate-from-applied-draft** (residual from the Tier-1 version-awareness work) — when a planner APPLIES a lever (reroute/OT/faster-operator) creating a draft whose placements differ from a plain re-solve, `whatIf.evaluate` re-sequences from BASE inputs and omits the applied lever — so a follow-up "evaluate options" on that draft evaluates the PRE-APPLY plan, not what's on screen. NOT the operator beat (a plain re-solve, where evaluate's input re-sequence == the draft), and NOT fixed by threading `baseVersionId` (evaluate ignores a version's stored placements entirely). The real fix is deeper: have `evaluate` SEED from the viewed version's stored placements as its base. Defer (post-demo).
- [x] **At-risk remediation as a direct costed option-set — DONE.** The two-door work shipped the "See options" door that renders the costed option-set DIRECTLY (the surface gap the item named): `useSeeOptions` → `AtRiskOptionsCard` (runs `at_risk_remediation` on mount) → `WhatIfOptionSet` — the SAME card the line-down flow uses (selectable costed tiles, recommendation marked, demoted-non-options line, honest-unachievable `unremediable` verdict at [whatif-option-set.tsx:270](../packages/app/features/whatif/whatif-option-set.tsx)). One click from the at-risk order; the faster-operator option rides the path (`whatif.option.fasterOperator`). Commit **50a4d95** (two-door: See options + Evaluate via Copilot). (The conversational "Evaluate options" door also exists — both, per the item's "not only Copilot".)
- [ ] **Demo-data: the operator-root scenario isn't naturally triggered by the warm-start seed.** Part A (operator as a lateness root) + Part B (faster-operator lever) are built + live-verified, but the canonical `demo:reset` state never roots an order at `operator` — Ana at 0.85 is too fast to overflow. **Sharpened by the Tier-1 dry-run (empirical):** a GLOBAL operator slowdown roots at `resource`/`capacity`, NOT `operator` — slowing every op on Press A just **overloads the line**, so the at-risk ops are queue-bound (`binding_kind='resource'`) and the chain roots at `capacity`. The `operator` root only fires when a SPECIFIC op overflows its OWN window *before the line saturates* — i.e. binding ∈ {`working_window`,`release`,`origin`} with `perf<1` ([sequencer.ts:434](apps/api/src/modules/scheduling/sequencer.ts#L434)). Confirmed live: **Ana 0.50 global → `capacity` root (wrong); Ana 0.30 with a tight line-head Press-A order → `operator` root (right)** — and only then did the Copilot give the operator HOW narration ("Ana at 30% of standard → inflated effective cycle → window overflow → late"). So the operator beat needs a TUNED SEED — a dedicated firm Press-A order with a tight due + Ana slow enough that THAT op overflows its window before the line queues — **not** a global slowdown poke. **Decision for RG** (demo narrative): how slow is Ana / which order carries the story.

## C. Realism batch (post-phase-6 — `POST-DEMO-REALISM-PLAN.md`, shifts first)
Ranked by recognition-value × feasibility on the heuristic.
- [x] **1. Shift/calendar model + Week view + Date navigation (THE KEYSTONE)** — DONE (web; native deferred to D). Calendar-aware placement, OT meaningful (seed loaded), day-axis = working window, week view + date nav with calendar-derived gaps. Engine committed; week view built + web-verified.
- [x] **2. Material-arrival timing (PV-22 → FG-3001)** — ACTIVE. Upgrades collision 3 from seeded anchor to computed (weld op can't start before PV-22 available). **Owner = scheduler material gate (D36/§4.8), NOT net-requirements** (component availability is the scheduler's gate; NR does finished-good netting only). Buy-component scope only — lightweight requirement link + seeded `availableAt` + `SequencerItem.earliestStartMs` floor in `placeJob` (composes with the calendar cursor) + at-risk on shortfall. Same `availableAt` threaded into both baseline arms (phantom-early-material trap). Seed flips PV-22 from anchor→linked-component-with-late-availability; slip computes. **Simplifications deferred to production — see the "Material gate" subsection under Future phases.**
- [x] **3. Inspection-station capacity** — DONE. — model CMM/leak-test as finite resources ops compete for (reinforces the leak-test story; entities exist).
- [x] **4. Min run-length** — DONE (minBatchQty in resource_type_config, run-to-minimum, config-driven proof passed).  — no tiny batches between big ones (constraint or seed-discipline).
- [x] **5. Operator performance modifier** — DONE (consumed operator.performanceFactor + resource_operator_assignment, divide convention, launcher lever).  — operator's performance modifies effective cycle time (links Workforce to schedule timing).

## D. UI polish (late — against settled content)
- [ ] **Fix UI components** — general look/refinement of the screens. Do AFTER realism (which changes screen content) so polish isn't redone.
- [ ] Apply the typography scale / board type-map / density pass consistently if any screens still lag the standard.
- [ ] **Native (Expo) verification pass — all screens** — verify rendering (not just `tsc`/compile) on a phone viewport across every surface, as part of the screen-refinement pass. Includes the **week view** (dense — compression likely needs narrow-viewport behavior) and the Copilot. Web is verified per-feature during the build; native is batched here to do once against settled screens.

## E. Deferred follow-ups (nice-to-have / if time allows)
- [ ] **SSE token-streaming for Copilot** (SKIP-65) — currently JSON-return + "Thinking…" pending state (EventSource is GET-only, tool-loop non-streaming). Watch the "Thinking…" feel in rehearsal; if it drags, this becomes polish (POST-based/chunked streaming later).
- [ ] **Real-time push (invalidation)** capability — SSE invalidation bridge (architecture approved in `POST-DEMO-REALISM-PLAN.md`). Enhancement to a moment that already works via `refetchOnMount`. Build only if time remains after realism.
- [ ] **Name the "Copilot"** — branding decision, defer to staging.

## F. Staging / run-of-show (the final pre-demo work)
- [ ] **Seed fully wired + coherent** — the Magna-Mexico scenario reconciling across all six views (`SEED-SCENARIO-SPEC.md` + historical-outcomes addendum).
- [ ] **Run-of-show** — the demo script: which collision, which screen, in what order; drive-the-drift-then-show-the-lift sequencing (frozen baseline reads zero until learning runs).
- [ ] **Talk track / honest framings** — "seeded data, real working system, your reality in the pilot"; baseline = "the lift our intelligence adds" not "vs your manual process"; the heuristic-stands-in-for-the-optimizer framing; the 5 demo-doc truthfulness edits.
- [ ] **`recorded` fallback ready** — narration AND the scripted conversation can run on `recorded` if live Groq misbehaves in the room.
- [ ] **Rehearsal** — full walk on the demo machine; confirm `demo:reset` restores clean baseline; watch latency/feel. **Runtime-verify the two code-verified-but-not-clicked items:** (a) the "See options" line-down control actually fires in the browser (closed `[x]` on a code-read of the wiring, not a live click — see A); (b) per-option narration scopes per-card at runtime (left `[ ]` open — the shared-component fix looks present in code but is LLM-driven, so only a live run confirms it). Both need a real click + live LLM here.

---

## G. Admin-config write paths (shift-model fields — DOING NOW: #1 + #2)
The shift model extended schema/engine but didn't carry new fields to the admin write paths, so they fell outside the otherwise-clean config pattern (DataTable + Popup form). Closing the cheap, visible gaps on EXISTING screens now:
- [ ] **#1 — `calendar.workingDays` in the Calendars form** — add to create/update calendar schema (currently `.strict()`, excludes it) + a weekday multi-toggle in the existing Calendars form. Closes a visible half-feature (the form already edits shift times + holidays but not which weekdays are open — pinned to seeded [1–6]). Smallest.
- [ ] **#2 — `resource.otCapMinutes` + Tier-B cost rates in the Resources form** — add both to create/update resource schema + numeric fields in the existing Resources form. **Bundle the cost rates** (same seed-only gap, same form, and cost is demo-visible via cost/unit + baseline cost deltas — the more important half). One schema + form pass for both.

---

## Future phases / productionization (post-demo — not in the 10 days)
> **Convention:** capture ALL deferred items here, even production-only ones, so nothing lives only in conversation memory.

> **Overarching platform tenet — everything is configurable (D42 / A7).** The standing architecture requirement: anything that is policy or preference (not physics) is **configured, not hardcoded**, and resolves hierarchically through the one config framework — **tenant → plant → (line/resource where coherent)**. The framework reaches sub-plant; each setting group declares how far down it is *coherent* (the policy-vs-physics boundary — see `CONFIG-FRAMEWORK-DESIGN.md`). Several items below are **instances** of this tenet rather than separate mechanisms: the KPI / dashboard config (Group 3), the optimizing-engine parameters incl. CP-SAT (Group 4), the `resource_type_config` sub-plant write-paths, and the Constraints control panel (the configure-and-see capstone). They share the resolve→cascade→reset→version→audit plumbing; none needs new plumbing. Net effect: "is X configurable per client / plant / line?" is answered **structurally** (yes, via the framework), and the work is wiring each group's fields + write-path, not inventing config.

- **#3 — `resource_type_config` admin screen** (DEFERRED) — net-new admin surface (Admin → Config → "Resource types": DataTable + Popup + controller/endpoints) for `splittable` + type-level `otCapMinutes`. Correctly seeded for the demo (line non-splittable, cell splittable, 120 min/day OT); never edited live in the demo. Per-client config matters at pilot. Defer — it's a whole new screen, not extending an existing one.
- **Shift-model config completeness** — once #1/#2/#3 land, a client can fully configure their operating model (shifts, working days, OT caps, splittability) from admin, per D48 (safe defaults, complete on install, configurable per client).
- **Calendar exceptions — "open a normally-closed window" (e.g. run 1 shift this Sunday).** The working calendar is a **hard** constraint on regular capacity; it has escape valves of increasing weight: (1) **OT** — stretch an already-open day past its normal end (soft, costed, the OT-aversion factor) — **BUILT**; (2) **open a normally-closed window** — staff/schedule a normally-closed period (e.g. a recovery Saturday/Sunday shift) as *planned regular capacity*, NOT overtime premium — **NOT built**; (3) **change the recurring pattern** ("we now run Sundays") — a config decision, not a scheduling lever (covered by the #1 `workingDays` admin field). The middle one is a real Tier-1 lever (plants run weekend shifts to recover when behind) and is **distinct from OT** (OT = more hours on an open day; this = open a closed day). **Model it as the symmetric inverse of a HOLIDAY** — the calendar already supports date-specific *closed* exceptions (holidays on a normally-open day); this is the same mechanism in the other direction (a date-specific *open* exception on a normally-closed day). Two pieces: (a) the calendar-data-model addition (date-specific open-exception windows, mirroring holidays) + (b) a what-if **lever** ("open this window to recover order X", costed, alongside reroute/OT). **Cheap-or-not depends on how holidays are currently modeled** — if holidays are already date-specific exception rows, the open-direction is a small extension; confirm before scoping. Post-demo unless a run-of-show beat specifically needs "recover by opening a weekend shift."
- **Constraints control panel (toggle constraints + set parameters on/off) — TRACKED, LOW PRIORITY.** A single "configure the operating model" screen: per-constraint **enable/disable toggle** + **parameter editor** (OT cap, min-batch, working days, campaign rules, objective weights), grouped by domain, with safe defaults + cascade + audit. **Demo value:** *shows* (not just tells) that the platform is configurable per client — open the panel, toggle a constraint, re-solve, watch the plan respond — directly supporting the D48 "installable by any manufacturer, safe defaults, configurable per client" story. **It's the CAPSTONE of the configurability work, not a standalone screen:** needs (a) the **on/off `enabled` flags per constraint** (most constraints are parameterized-but-always-on today — adding toggles is real work), (b) the **L2→L3 admin write-paths** (the section-G items: OT cap, working days, resource-type config — so parameters are editable), (c) framework cascade/audit. Cheapest compelling version: surface the constraints already configurable (OT cap, min-batch, working days, weights) as a read/edit panel, defer the harder on/off toggles. **Scope after the configurability audit** (DONE — see `CONSTRAINT-INVENTORY.md`: only objective-weights + reporting-window are full-L3 cascade+audit; autonomy is L3 tenant-only with the lone real on/off flag `boundedAuto`; calendar + per-resource OT are admin-editable but flat; everything else is L2 seed-only) + the G write-paths. **Near-term blocker confirmed: `minBatchQty` (in `resource_type_config`) has NO admin CRUD at all — pure seed-only — making it the single clearest section-G configurability gap** (the OT-cap *per-resource* override is already editable via the Resources PATCH; the OT-cap *type-default* and min-batch are the seed-only ones). The `resource_type_config` admin screen (#3 above / "Monitor surfaces") is the write-path that closes it. Low priority now; tracked so it isn't lost.
- **Phase 7** (`PHASE-7-NOTE.md`) — conversational apply (act on an explored scenario via the conversation, with explicit confirm preserving D26).
- **Full optimizer** — CP-SAT / OR-Tools flexible-job-shop with all constraints (shifts, tooling, materials, cost-as-objective, cross-line dynamic re-optimization). Drops in behind the `external_solver` binding / contract — architecture is already a drop-in. Its own planning artifact. **Two halves, per the configurability tenet:** the **provider half** is the `external_solver` binding (the engine swap); the **config half** is **Solver Policy** (`CONFIG-FRAMEWORK-DESIGN.md` Group 4) — the **configurable engine parameters** (solve budget / time limit, optimality gap, search workers, deterministic seed, engine selection) resolved hierarchically (tenant→plant, selected params per-resource) and **stamped at solve so determinism (D2) holds**. The objective **weights** stay in Objective Policy (Group 1); Solver Policy tunes *how* the optimizer runs, Objective Policy sets *what* it optimizes for.
- **Per-tenant LLM provider config** to DB (provider/model/params/custom-URL as data, secrets as secrets-manager references, no-deploy) — productionizes the phase-5 env-based selection.
- **Prompts → DB / per-tenant config** — narration + conversation prompts currently code constants (hash-derived `promptVersion`); move to versioned config / per-tenant overrides at productionization (same trajectory as the LLM provider config).
- **Rolling / multi-week planning horizon** — the week view DISPLAYS any version's horizon already (production-ready as-is); generating longer/rolling horizons (extend forward, re-plan as actuals arrive) is engine/planning-cadence work, separate from the view.

### Material gate (D36/§4.8) — what the demo build SIMPLIFIES, deferred to production
> C2 builds the **buy-component material gate only** (D36): a lightweight requirement link + a seeded `availableAt` + an `earliestStartMs` floor in `placeJob` + at-risk on shortfall. The following are deliberately NOT built for the demo and are captured here:
- **Real material-availability input (§4.8 / D35)** — the demo **seeds** `availableAt` (PV-22 lands at T). Production computes it from **on-hand inventory + inbound scheduled receipts** via the D35 ERP/inventory/MES input. Seed-now, real-input-later: structure the gate so the real source drops in without reshaping it.
- **Full BOM explosion (§5.1, SKIP-45)** — the demo uses a lightweight component→consuming-op requirement record (single FG-3001→PV-22 link), NOT a real multi-level BOM. Full BOM (`part` BOM structure, recursive explosion) stays deferred. **Note:** the C2 `material_requirement` table is the **interim** requirement source — scheduler-side to keep the master-data boundary clean (no premature half-BOM). When the real master-data BOM is built, the gate reads requirements from it and `material_requirement` retires.
- **D37 make-component precedence / dependent-demand** — NOT built. PV-22 is a *buy* component (material gate only). Make-components (sub-assemblies whose own production must precede the parent, multi-level dependent demand, op-precedence ordering) are the heavier D37 feature — deferred. (`opSeq` is carried on items today but never used to order; real precedence is part of this.)
- **Component shortfall remediation as what-if options** — the demo surfaces material-driven lateness as at-risk; expediting/alternate-sourcing/partial-build options are future.

### Product structure: BOM + packaging
> Two related product-structure gaps, captured together because they share the master-data home — but they are at **different maturity**: BOM is **spec'd, not built** (a build behind a finished contract); packaging-as-production is a **genuine gap** (not modeled, not spec'd) and needs a spec decision before it's a buildable item.

- **Bill of Material (BOM) — spec'd, not built; activate the master-data design.** Multi-level BOM (topology, `qty_per`, scrap factor, effectivity, where-used, recursive explosion) is **owned by the Master Data module** and exposed as a contract — fully designed in `master-data-module-spec.md` (MD5, D37) and consumed in `net-requirements-module-spec.md` (NR1: dependent-demand explosion → component netting → lot-size → schedule with precedence). The demo deliberately simplified to a **single-level material gate** (the interim `material_requirement` link, see "Material gate" above) and **does not** do multi-level explosion or make-component (D37) precedence. So this is a **build behind an existing contract design**, not a redesign: when the master-data BOM lands, the scheduler's material gate reads requirements from it and `material_requirement` retires (per the Material-gate note). Architecture already accommodates it (contract-first boundaries A8, no cross-module joins). **Sub-items:** (a) master-data BOM entity + explosion service (MD5); (b) scheduler reads BOM-derived requirements (retire `material_requirement`); (c) D37 make-component dependent-demand + op-precedence (the heavier half); (d) net-requirements consumes BOM explosion (NR1). Per the configurability tenet, BOM revisions/effectivity are config-governed (ECN/ECR, versioned, never edited in place — MD revision control).
- **Packaging — NOT modeled, NOT spec'd (the genuine gap; needs a spec decision first).** "Packaging" today appears in the platform docs only as **commercial** packaging (module entitlement, A7/AQ3) — **never as a production concern.** Packaging-as-production is unmodeled: **packaging operations** in the routing (pack/label/palletize as routing steps with their own cycle/changeover), **packaging materials** in the BOM (cartons, dunnage, labels as `buy` components the material gate must cover), **packaging resources** (pack stations / lines as schedulable resources, possibly a constraint), and **pack-spec config** (per-customer pack quantities, labeling, automotive returnable-container rules). **Open modeling decision before this is buildable:** most likely it **extends existing master-data** (packaging ops = routing steps; packaging materials = BOM components; pack stations = resources) rather than a new module — but whether pack-spec/returnable-container logistics warrant their own module (alongside the candidate "supply-chain logistics" module in A7) is the call to make. **Action: write a short packaging spec** (entities + which module owns each + how it rides BOM/routing/resources/material-gate) and confirm the extend-master-data vs new-module decision, **then** it becomes a placed backlog item. Until then it's recorded here as *needs spec*, not *ready to build*. Once modeled, pack rules are configurable per the tenet (per-customer/plant pack specs through the config framework).

### Master-Data Layer 0 — BUILT (close-out); follow-ups below
> **Layer 0 (effectivity + revision + audit substrate) is BUILT + verified** — `docs/master-data/CLAUDE-CODE-BRIEF-MD-LAYER-0.md` + `MASTER-DATA-LAYER-0-SCOPE.md` (D-L0-1…7). Shipped across: C1 `master_data_audit` + Pattern-B audit (`622fec9`); C2 part version columns (`7089165`); C3 routing version columns + `part_no` (`321ed73`); C4 `btree_gist` exclusion constraints + `db:migrate:custom` (`1126299`); C5 `MasterDataResolver` resolve/revise + `masterdata.read` 1.4 + Pattern-B atomicity (`1625810`); C6 part-identity consumer migration to resolve-as-of (`179335d`); C6b never-edit-in-place + `getPartVersion` snapshot-read coherence (`a0a24a1`); C7 re-seed + reset-flow custom-migration wiring + DoD close-out. §9 DoD all green. The deferred pieces on top of it:
- **Admin parts/routings list shows ALL versions → add a `listOpenParts`/open-versions read-op.** `masterdata.read`'s `listParts` (and the routings list) return **every** version row — required because the scheduling display maps (`partNoById = listParts → id→partNo`) must resolve any frozen-snapshot version id. But the ADMIN list screens use the same `listParts`, so after an edit (which creates a new version) they show BOTH v1 and v2 for one `part_no`. Add an **open-versions-only** read (`listOpenParts` / `listOpenRoutings`, `effective_to IS NULL`) for the admin list surfaces, leaving `listParts` (all versions) for the id→part_no resolvers. Inert for the demo (`demo:reset` restores the 1-version baseline) — a Layer-2 UI/read refinement, not a correctness bug. Code: `master-data.repository.ts` `listParts`; consumed by `master-data.service.listParts` (admin) + `scheduling.service.ts` `partNoById` maps.
- **Layer 2 draft/publish authoring lifecycle** — 6b (never-edit-in-place: admin `updatePart`/`updateRouting` route through `revise*`, creating a new effectivity-dated version) is the **foundation** the draft/publish lifecycle builds on. Explicit `revision`/`effectiveFrom` (chosen in 6b) *extends* into draft/publish, whereas an auto-anchor-to-now would have to be undone — so no rework. Draft/publish itself (parked for Layer 2 BOM) is the future build.
- **Revise admin-edit UI form (the 6b UI hedge)** — 6b took the **UI hedge**: the service accepts `revision`/`effectiveFrom` as optional and auto-derives them (next revision label, effective now) when the admin edit form omits them, so the never-in-place redirect landed without an admin-form change. Finalize the form: surface explicit **revision label + effective-from** inputs on the part/routing edit screens so revisions are meaningful (ECN-tied), not implicitly auto-labelled. Contracts already accept the fields (`updatePartSchema`/`updateRoutingSchema` optional `revision`/`effectiveFrom`).
- **`revise*` HTTP-endpoint guard (from Commit 5)** — the `revisePart`/`reviseRouting` resolver ops are not yet exposed as their own HTTP endpoints (edits reach them via the existing `PATCH` redirect, which is already guarded `JwtAuthGuard + ConfigureGuard` and passes the JWT `actor`). If/when `revise*` is exposed **directly** over transport, it must carry the same guards (`JwtAuthGuard + ConfigureGuard`, master-data-admin) and thread the JWT `actor` onto the audit trail — native-SoR only.

### Master-Data Layer 1 — BUILT (close-out); documented futures below
> **Layer 1 (part-to-fullest: part-core completion + UoM factors + physical attrs + plant override layer + MD9 alias resolution) is BUILT + verified** — `docs/master-data/CLAUDE-CODE-BRIEF-MD-LAYER-1.md` + `MASTER-DATA-LAYER-1-SCOPE.md` (D-L1-1…6). Shipped across: C0.5 `org.read` 1.1→1.2 (`validateCustomerIds`/`validateProgramIds`, `5fb6f10`); C1 `make_buy` + customer/program refs, migration 0026 (`828ec3c`); C2 `tool_family` + `shared_attributes`, migration 0027 (`61a7cbb`); C3 `uom_conversion` + `getUomFactors` + guarded factor copy-forward, migration 0028 (`835ff5a`); C4 `part_plant` per-plant override layer + `resolvePart({ plantId })` (prefer-plant-else-global + shallow `shared_attributes` merge) + GiST non-overlap, migration 0029 (`a1cea20`); C5 `plant_part_mapping` + `resolvePlantPart`/`resolveCustomerPart` (MD9 → typed `UNRESOLVABLE_PART_REF`) + GiST, migration 0030 (`f09074d`); C6 `masterdata.read` 1.4→1.5 + guarded admin transport for the override/mapping/factor writes + `make_buy` required in `createPart` + `factor` `numeric`, migration 0031 (`494dfcc`); C7 factor-as-string boundary + re-seed + DoD close-out (this commit). §7 DoD all green; `resolvePart` without `plantId` proven byte-identical to Layer 0; demo schedule unchanged (1043 scheduled ops). The documented futures on top of it:
- **First-class exact-decimal computation for UoM factors (the `getUomFactors` `Number()` cliff).** `uom_conversion.factor` is stored `numeric` (exact) and travels as node-postgres' native decimal STRING all the way to the mapper — proven digit-for-digit (a 30-significant-digit factor round-trips byte-identically). The **single, deliberate** narrowing to a JS `number` is at the DTO boundary (`MasterDataResolver.getUomFactors`), which caps precision at ~15–17 significant digits. For the demo's factors (e.g. COIL→EA = 500) this is exact and irrelevant. To make **conversion arithmetic** first-class exact-decimal (when MD4 consumers actually multiply `alt_qty × factor`): keep `factor` a string/`Decimal` end-to-end — change `UomFactorDto.factor` to a decimal string (or a `Decimal` type), do the multiply with a decimal library, and make the write input (`addUomFactorSchema.factor`) a string too (today `z.number()` — a JSON number is already float-parsed at the HTTP edge). No global OID-1700 parser is registered precisely so this stays a single explicit boundary, not a hidden per-column coercion (`db/pool.ts`). Until a consumer does factor math, the `number` DTO is sufficient.
- **Per-plant UoM factors.** `uom_conversion` factors ride the **part version** only (keyed to `part_id`), copied forward on `revisePart` (guarded on the base UoM). A plant needing a *different* factor for the same part has no override path — factors are not part of the `part_plant` override layer (§4E covers named attrs + `shared_attributes`, not factors). Future: extend the override layer (or add a `plant_uom_conversion`) so `getUomFactors` can layer a per-plant factor as-of, mirroring `resolvePart`'s plant layering. Out of scope for Layer 1 (explicit stop condition).
- **Deep/nested `shared_attributes` merge.** The `part_plant` override merges `shared_attributes` by **shallow top-level key-merge** (`{ ...global, ...plant }`; plant `null` = inherit; a nested object **replaces wholesale**, no recursion). If plant overrides need to patch a *sub-key* of a nested object without restating the whole object, a deep/recursive merge is required — deliberately deferred (explicit stop condition). Code: `MasterDataResolver.mergeSharedAttributes`.
- **Multi-customer part mapping table.** Customer resolution (`resolveCustomerPart`) reads the **inline** single `customer_id`/`customer_part_no` on the part version — one customer ref per part. A part sold to multiple customers under different customer part numbers needs a dedicated `customer_part_mapping` table (windowed, `(customer_id, customer_part_no) → part_no`, mirroring `plant_part_mapping`). Future; the current inline fields cover the single-customer demo. 

### Inspection capacity (C3) — demo models it as a STATION, spec wants a cert-skill pool
> C3 builds inspection capacity as a **station-as-resource** (model a): an inspection station resource + an inspection routing op + weld→inspect precedence via the C2 earliest-start floor. The station is the **hard** finite constraint (ops queue for the booth, visible as a Gantt lane). This deviates from the spec:
- **D29/D54 prescribe a cert-skill pool per shift** (finite capacity-by-skill counter — N qualified inspections per shift), NOT a physical station resource. Chosen station-as-resource for the demo because it's **concrete and visible** (you can see ops queue for the booth on a lane); the cert-pool counter is invisible (a number, no lane).
- **Certifications stay soft/advisory** — LEAK/CMM/TORQUE certs remain Workforce-Coverage-view-only (View 3, gap detection, D54), decoupled from the sequencer, as today. In model (a) the station gates *finite capacity* (hard); the cert gates *who can staff it* (soft overlay).
- **Production version** — reconcile to the spec's cert-skill-pool model (hard cert-skill capacity per shift) OR keep station-as-resource and formally fold cert-staffing into the sequencer (hard cert gate). Either is a productionization decision; the demo's station model is the deferred simplification.

### Operator performance (C5) — future refinements
> C5 builds operator performance as a **consumed** input: `operator.performanceFactor` (efficiency rating, 1.0=standard, >1.0 faster, `effectiveCycle = baseCycle / factor`, run-time only — setup not divided) + a scheduler-owned `resource_operator_assignment` (consumed, never optimized — labor boundary intact). Settable via launcher. Deferred refinements:
- **Task-specific performance** — the factor is on the **operator** (she's a 90% operator everywhere). Real skill is task-specific (faster on familiar stations). Future: move/override the factor onto the **assignment row** (operator-resource pair) so the same operator can be faster on one line than another. The assignment row already exists; it'd gain an optional factor override.
- **Recurring / shift-coded assignment windows** — the assignment window is a `[effectiveFrom, effectiveTo]` timestamp range now. Future: recurring patterns ("operator O works Shift A every weekday") or shift-of-day codes, rather than explicit per-window rows.
- **`perfFactor` on the placement + board tooltip — BUILT (this session).** The op card shows the operator the engine applied: name · rate · % of standard · timing effect ("85% of standard — ~18% slower; this op's cycle reflects it"), same-source via a shared `buildOperatorResolver` (the card can't disagree with the engine). Unassigned reads honestly ("running at standard — no named operator"), not as a vacancy.
- **Real roster source** — the assignment is seeded for the demo; production fills `resource_operator_assignment` from a real roster/MES (the §4.8 consumed-input seam, same as material availability).
- **Planner assign/switch operator lever — BUILT (this session).** A resource-grain "assign operator" control on the lane panel (+ product POST/DELETE `/admin/scheduling/operator-assignments`) writes `resource_operator_assignment` + a "re-solve to apply" affordance (no auto-re-solve). Validation reflects floating operators: home-plant NOT enforced (cross-plant allowed; home plant informational), per-operator double-booking rejected (`OPERATOR_DOUBLE_BOOKED` — can't be two places), per-resource one-by-construction. "No cross-plant mid-shift" deferred (needs the shift model).
- **Crew per line** (deferred) — multiple operators on a line → combined factor + summed labor cost. Today one operator per line (labor cost = single rate).
- **Labor-as-capacity / multi-line tending** (deferred) — operator tending-capacity vs. line attention-demand (one operator tends several automated lines; one per manual line). The double-booking guard is the manual-case first step; relaxes to capacity-based. (Full framing in ENGINE-CONSTRAINTS Part 5 + 1D.)
- **Operator-assignment as a what-if remediation lever** (deferred) — a scored option ("assign Sofía 110% to recover this late order, +$X labor") alongside reroute/OT; build with the at-risk direct-option-set item (section B).

### Monitor / display surfaces (read-only — derived & external values)
> **Principle: configure vs. monitor.** Admin/config screens are for values a HUMAN SETS (editable). A separate class of values is DERIVED by ML or FED by external systems — a human should SEE them (trust, audit, understand) but NOT edit them (editing a learned/measured value is meaningless or breaks the loop). These want **read-only monitor surfaces**, distinct from editable config. Today these values display only **in-context** (on the board, in panels); production wants consolidated views. None are demo-critical (in-flow board display already shows them where they land, which demos better than a static table).
- **Learned-parameters view** — all current ML-learned cycle times (op, resource, std→ml value, # actuals, confidence, settled/drifting). Today per-op on the board; consolidated read-only "what the system has learned." Strongest candidate.
- **Predictions view** — active wear/forecast predictions (crossing time, confidence) consolidated, not just per-board.
- **Performance display** — operator/resource effective performance: the **seeded-from-external baseline** AND the **derived-from-actuals observed** value side by side (e.g. "seeded 85%, observed 80% over 30 cycles"). Read-only for the derived part; the seeded baseline stays editable on the Operators form.
- **External-inputs view** — operational data feeding the scheduler from upstream systems: material availability (ERP/inventory), operator assignments (roster/MES), actuals (MES). Displayed, fed-not-edited.
- **`resource_type_config` admin screen (#3, GREW)** — now holds `splittable` + type-level `otCapMinutes` + `work_center` config + **`minBatchQty`** (C4 added it). It's become the **resource-type operating-profile** config (how a press/cell/work-center behaves). Net-new admin screen + endpoints; all values correctly seeded for the demo, so still deferred — but more substantial than first scoped.
- **Operational-vs-config is intentional** — `material_availability` and `resource_operator_assignment` are operational data (launcher for demo, ERP/roster for production), deliberately NOT admin-config screens. Correct (you don't hand-edit "when did the part arrive"); stated so it's a conscious choice.

### ML engine — production build TODO
> The demo uses deterministic stand-ins (OLS linear-trend predictor; seeded performance/availability). Production replaces these with the real ML/learning engine. Captured as a running TODO so the demo→production ML gap is explicit.
- **Operator performance derivation** — performance is **seeded from an external system initially, then derived from past performance** (observed output vs. standard over time). The demo seeds `performanceFactor` static; production: ingest the external baseline, then continuously re-derive the observed factor from actuals (same learning-overlay shape as cycle times — measured, confidence-weighted, settled/drifting). Surfaces in the Performance display (seeded vs. observed).
- **Real predictive model** — replace the OLS linear-trend wear predictor (P4 stand-in) with the production ML model (proper forecasting, feature inputs, validated accuracy). Drops in behind the existing predictor seam; confidence×tier gate (P4) stays the authority boundary. **Requirements: `docs/ML-PREDICTOR-REQUIREMENTS.md`** — the in-contract drop-in spec for the `predict()` seam (MLR-* requirements + D-ML-* open decisions): owns the parameter forecast (value, horizon, calibrated confidence) while the gate/overlay/bounds/determinism stay engine-owned; covers calibration, explainability (A18/A19), versioning, the per-op vs per-tool grain decision, shadow-mode rollout with OLS as the floor, and promote-to-gating acceptance criteria. (Subsumes the calibration / monitoring / provider-seam / graduated-autonomy bullets below.)
- **Real cycle-time learning** — the std→ml overlay is the learning mechanism; confirm the production learning pipeline (actuals ingestion from MES via D35, the derivation cadence, the convergence/settle logic, confidence model AQ8).
- **Confidence model calibration (AQ8)** — the agent confidence model needs real calibration against measured outcomes (the demo uses a heuristic confidence = samples·R²·horizon-decay). Production: calibrate so confidence×tier auto-commit thresholds are trustworthy.
- **Accuracy measurement / model monitoring** — retain predictions vs. actuals (the prediction-accuracy substrate) and build the model-monitoring view (drift, accuracy over time) — production needs to know if the model is degrading.
- **ML provider/infra abstraction** — like the LLM provider pattern, the ML engine should be a pluggable provider (train/serve infra abstracted) for cloud portability. Define the ML provider seam.
- **Graduated autonomy by measured outcome** — the confidence×tier autonomy must be EARNED from measured accuracy (a tier-1 param auto-commits only after the model proves itself on that param class). Production: the mechanism that ratchets autonomy up as outcomes validate it.
- **Optimizer selection (AQ6)** — the full CP-SAT/OR-Tools optimizer (separate from ML, but the other "real engine" swap) drops in behind the `external_solver` binding; engine selection is an open architecture question. The selection itself + its run parameters are **Solver Policy** config (Group 4) — see the "Full optimizer" item above for the provider-half / config-half split.

### Override "pin" / standing veto (autonomy-config / override family — DEFERRED DECISION)
> **What it is.** A *per-lane (per resource×param) standing veto*: once a human overrides a wear forecast, the confidence×tier gate is **disabled for that lane until the human re-approves** — auto-commit can't fire again on its own, regardless of how confident the next forecast is. Distinct from today's *one-shot* override.
- **What ships today (intentional, NOT a pin).** `revertPrediction` + `dismissPrediction` are **one-shot**: they undo/snooze the current forecast and re-arm only on *materially-worse* new data (the supersede-on-worsen path). `reopenPrediction` (the manual path back) **deliberately does NOT pin** — it returns the forecast to `queued` as a proposal, and a subsequent *confident* actual can re-adopt via the auto-gate **by design** (manual reconsideration vs new information are different triggers). See the re-open / revert / set-aside work (commits `72ac064`, `ce01f3c`, `b2db8e5`) and `requireAdopted`/`requireSetAside` in `learning.service.ts`; the no-pin behavior is asserted in `reopen.spec.ts` (reverted→queued, never an auto re-adopt at re-open time).
- **Pull-forward trigger.** A client asks *"why did it re-act after I said no?"* — i.e. they expected an override to *stick*, not to be re-decided by the next confident reading. That's the signal the one-shot model isn't enough and a standing veto is wanted.
- **Where it belongs.** The **autonomy-config / override family** — a per-scope (lane/param) flag alongside `tier1AutoThreshold` / `boundedAuto` (autonomy is already L3 tenant-only per `CONSTRAINT-INVENTORY.md`; this would be a finer-grained, lane-level override). Pairs with the **Graduated autonomy by measured outcome** bullet above (earned-up) as its inverse (human-pinned-down). Likely a new disposition or a `pinned` flag the gate checks in `gateDisposition`, plus an un-pin (re-enable autonomy) action.
- **Why deferred.** The one-shot override already gives the human a clean escape hatch + a manual way back; the standing veto is only warranted once the re-adopt-on-new-info behavior is actually felt as wrong in front of a client. Revisit then.

### Actuals rollup — versionMetrics deliberately not moved (DECISION, not a gap)
> When the continuous actuals folds were centralized into `ActualsRollupService` (commit 9ea0ee1), `versionMetrics` (the per-version scorecard fold) was **intentionally left in `SchedulingService`** — this is a settled decision, not an oversight.
- **Why it's fine where it is:** it reads actuals via the `learning.read` **contract**, never raw learning tables — so O1/O2 already hold. The goal was "nothing reads raw actuals *tables* + duplicated math gets one home," not "physically relocate every actuals read." Both the cockpit path (now via the rollup) and the per-version path (via the contract) satisfy that.
- **Why moving it would be worse:** it's **single-use** (not duplicated like the cockpit folds → no DRY gain) and it's **entangled with `latenessChainsFor`** (a lateness-narration helper shared by workList + 2 version-detail methods, 4 callers total). Moving it cleanly would drag a non-actuals helper into the "actuals rollup," **muddying the boundary** the refactor exists to keep clean, and touch ~8 unrelated call-sites under the parity gate for no functional gain.
- **Do not "tidy" this later** by moving it into the rollup — that reintroduces the boundary muddiness (a lateness-narration helper inside an actuals rollup) for zero functional gain. If a genuine future need ever makes the per-version fold *duplicated*, revisit then — not before. The code comment at `versionMetrics` in `SchedulingService` records the same decision on-site.

### Yield / Quality module (NOT modeled — the highest-value post-demo gap)
> The engine assumes **100% yield**. There is no Quality module. This is the single most consequential product gap identified — it is *both* a correctness gap *and* a missing predictive domain, and it is what a Tier-1 ops person would feel first in a pilot (floor reality is never 100% good).
- **Correctness gap (current):** planned **good-output quantity is overstated** — the plan schedules nominal quantity with no scrap/rework accounting, so true completion, true throughput, and downstream material consumption are all computed against an unreal 100%-good assumption. Real plans routinely miss because of this.
- **OEE Quality leg is SEEDED, not MODELED:** OEE = Availability × Performance × Quality (api-spec §12.6). For the demo the Quality leg is honest-but-synthetic — seeded `historical_outcome` quality + `scrap_qty` injected into `execution_actual` rows so per-version Quality computes. **True OEE depends on a real yield model;** the demo's Quality number is a stand-in, the same way the wear predictor is an OLS stand-in.
- **Missing predictive domain:** yield/scrap prediction (forecast a run's defect rate *before* it runs — this part, this line, this speed, historically scraps X%) is the Quality module's predictive job (D3 prediction target #4, scrap/yield). Lets the plan account for rework loops and real good-output, not nominal. **Not built.**
- **Scope when built:** a Quality domain that (a) owns scrap/yield data + process capability, (b) feeds *expected good-output = qty × yield* into planning (fixing the 100% assumption), (c) computes the OEE Quality leg from real measured quality, (d) predicts yield forward. The scheduler **consumes** expected-yield through a seam (same consumed-input pattern as material/operators) — it does not own quality.
- **Likely the #1 post-demo build.**

### Scheduler hard-constraint gaps (AUDITED this cycle — evidence in `CONSTRAINT-INVENTORY.md`)
> The constraint audit (read-only, code-verified) flipped several spec'd constraints from "questionable" to confirmed-**NOT built**. Captured here as evidenced gaps, not assumptions.
- **D28 sequencing / campaign rules — the #1 hard-constraint gap (NOT built, confirmed).** None of the four legality types exist: **required-ordering, contiguity, forbidden-transition (+ required cleanout), max-consecutive.** Changeover attributes are *modeled, not sequenced* (routing schema, SKIP-48); the only attribute-aware logic is a soft, firm-excluded forecast changeover-grouping bonus (`CHANGEOVER_BONUS_HOURS=24`). **This is a CORRECTNESS gap, not just a feature gap** — in automotive a wrong transition (e.g. paint dark→light without a purge, or skipping a cleanout) = **scrap**. Likely ranks **with the Yield/Quality module** for post-demo priority. **Demo-honesty implication:** claim *"reduces changeovers"* (the flat switch-count IS in the objective ✅), **never** *"enforces sequencing rules"* (it doesn't).
- **D8 per-pair changeover cost matrix (NOT built).** Changeover is in the objective only as a **flat switch-count** (weight 1) — every transition costs the same. The attribute-keyed per-pair cost matrix (transition A→B costs more than C→D; sequence-dependent setup time) is not built. Build alongside the D28 campaign/changeover work (same domain: the sequencer becomes attribute-aware for both legality and cost).
- **Schedule-commit approval gate (SKIP-46, NOT built).** The confidence×tier autonomy gate is **LIVE for learning predictions** (`gateDisposition` — tier × computed confidence auto-commits/queues wear forecasts), but `commit()` on a schedule version has **no approval check** (explicit SKIP-46 seam). **Talk-track:** lean the graduated-autonomy story on the **prediction path** (genuinely live, demoable), not on schedule-commit gating (which is a future seam). Approval *tiers* are seeded (Planner/Supervisor/Plant manager) but no rule routes a commit to them yet.

### Prediction ownership by module (the platform predictive roadmap — see `PREDICTION-OWNERSHIP-BY-MODULE.md`)
> Organizing principle: **prediction belongs to the module that owns the predicted thing; the scheduler consumes forecasts through stable seams and predicts only about the PLAN.** The vision is not "the scheduler predicts more things" — it's "each domain module gains predictive intelligence; the scheduler integrates ever-richer forecasts through the seams it already uses (material, operators, downtime)." Each is the same `observe-actuals → forecast → confidence-gate → propose → adopt-on-confirm` loop the tool-wear lifecycle already implements. Full detail in the standalone doc; summarized here so it lives in the master backlog (per the all-deferred-items-captured convention).
- **Demand / Net-Requirements** → demand forecasting (OEM release projection, CUM/netting — the central JIS/JIT problem). Net-requirements module is spec-only (see below).
- **Supply / Inventory** → material-arrival prediction (supplier-reliability forecast of `availableAt` with a confidence band) + supply-risk flagging. Material gate built (seeded `availableAt`); prediction is future.
- **Workforce** → operator-performance *derivation* from actuals + performance-trajectory forecasting (learning curve, fatigue trend). Performance is *consumed* today (C5); derivation is in the ML-engine TODO.
- **Quality** → yield/scrap prediction (the Yield/Quality module above). The highest-value gap.
- **Asset / Maintenance** → equipment-failure prediction → predicted downtime (same shape as injected `resource_downtime`, so a line-down becomes *predicted and pre-positioned*, not just reacted to); die-life; **tool-wear**. *Boundary note:* tool-wear is currently the **scheduler's** headline predictive beat, but architecturally belongs to **Asset** (predict wear → write a cycle-adjustment the scheduler consumes, like material/operators/downtime). Cleanup deferred; flagged so the boundary is intentional.
- **Scheduler (the integrator)** predicts only about the plan it owns — at-risk, what-if, cascade/ripple, bottleneck-migration, schedule-stability. Stays deterministic + authoritative; ML/forecasts feed it, never decide for it; the human confirms consequential changes.
- **Platform thesis:** PerduraFlow is a manufacturing-ops platform of cohesive domain modules, each owning its data + prediction, feeding a deterministic scheduling kernel that integrates them into one optimized, auditable, human-confirmed plan. Modular (adopt the scheduler first; add domain predictors over time). The architectural test for any new predictive feature: *which domain owns the predicted thing?* — that is where its prediction lives; anything genuinely ambiguous defaults to the scheduler.

### Performance / KPI dashboard (902 — the fuller manager/exec surface, NOT built)
> The cockpit and scorecard are deliberately **curated** to ~4 distinct-axis KPIs each (cockpit: On-Time/Utilization/At-Risk/OEE; scorecard: On-Time/Adherence/OEE/Cost — each a different decision axis, redundant ones excluded). The spec (production-scheduling-business-functional-spec §902) defines a **separate, fuller Performance/KPI dashboard** (manager/exec role) holding the broader operational KPI set. We explicitly **routed the non-curated KPIs here, off the cockpit/scorecard, and deferred building it.** Captured so the placement decision isn't lost.
- **KPIs that belong on the 902 dashboard (not the cockpit/scorecard):**
  - **Changeover time** — scheduling-addressable (the optimizer minimizes changeovers), a strong value-story KPI; the one candidate worth surfacing earlier if a demo beat needs it.
  - **WIP** — work-in-process inventory. **Possible real gap: confirm whether WIP is modeled/computed at all today** (it may not be — if so it's a modeling gap, not just a display gap).
  - **Scrap** — its own KPI (data is seeded for the OEE Quality leg anyway; cheap to surface). Tied to the Yield/Quality module above.
  - **Churn (D57)** — plan-stability (ops that moved between versions beyond a threshold); already computed in `versionMetrics` (api-spec §12.6) but not surfaced as a headline KPI.
  - **Throughput** — already on cockpit + scorecard; the 902 dashboard is its fuller home if a trend view is wanted.
- **Prediction-accuracy / AI-performance KPIs (§1003) — a SEPARATE surface, not the operational dashboard.** Proposal approval rate, top-proposal-accepted rate, override/edit rate, proposal-vs-outcome — the "is the AI trustworthy" metrics that gate graduated autonomy (A16/A17). These belong on a model-health / AI-performance surface, distinct from the operational 902 dashboard. (Related to "Accuracy measurement / model monitoring" under the ML-engine TODO.)
- **Why deferred:** the curated cockpit + scorecard tell the demo story; the 902 dashboard is the comprehensive operational view a manager/exec wants in production, not demo-critical. Build post-demo — partly **blocked on Q20** (which KPIs Magna tracks, at what level, with what targets/thresholds — still an open client question), since the dashboard should be built around the KPIs the client actually watches.
- **Configurable KPIs + dashboard composition (the configurability tenet applied to KPIs) — KPI / Metric Policy, Group 3.** "Define different KPIs and how to show them" is **not a bespoke build** — it's the config framework applied to metrics (`CONFIG-FRAMEWORK-DESIGN.md` Group 3). Three things become config data rather than curated code: (1) the **metric set** per surface (cockpit / scorecard / 902), (2) each metric's **target + threshold bands** (green/amber/red) — **resolvable to the line** (a lane-level OEE target is coherent below plant; contrast objective weights), (3) **dashboard composition** (which metrics, order, role/surface) read by the dashboard-registration framework (A7). Today's curation (4 distinct-axis KPIs each) becomes the **default composition**, overridable per tenant/plant/line. This is exactly what **resolves Q20 structurally**: rather than hardcode "the KPIs Magna watches," the dashboard is composed from configured metric definitions, so any client configures their own set/targets without a rebuild. Metric *definitions* (formula/unit/axis) are reference data modules register; the policy *selects and parameterizes* them. Sequencing: build with the 902 surface, on top of the existing Reporting Policy (Group 2) which already made the KPI *window* config.

### Net-requirements module (spec-only, NOT built)
- The whole **net-requirements module** (finished-good / independent-demand netting, D20 — CUM-based netting, PAB) is **spec-only, not built**. It is a separate module from the scheduler material gate (NR1 / D36 are explicitly distinct: NR = finished-good netting, scheduler = component availability). Build is a future phase.
- **Real-time push (invalidation)** — SSE invalidation bridge (`POST-DEMO-REALISM-PLAN.md`); enhancement to a moment that already works via `refetchOnMount`. (Also listed under E if attempted pre-demo.)

---

## Status anchor (where things stand)
- **REALISM BATCH (C) COMPLETE** — all five items built + verified, each with a config-driven proof (responds to its input, not staged): C1 shifts+week-view, C2 material gate, C3 inspection capacity, C4 min-batch, C5 operator performance. The schedule now behaves like a real constraint problem (working hours, material availability, finite inspection capacity, within-routing precedence, minimum batches, operator performance) — not a 24/7 toy. Each rode the foundation the prior built (cycle seam, cursor-floor, resource model, consumed-input pattern, resolver-callback).
- **Phases 0–6 built + verified** (engine + learning + prediction + what-if/baseline/narration + conversational layer). Demo feature-complete; the conditions (material, goal-seek, tool-wear, line-down, operator-performance) are all walked end-to-end, and the remediation/KPI surfaces are validated across both plants.
- **Remaining is mostly NON-engine** — the engine work that landed this cycle (line-down, operator-performance, infeasibility scoring, the two-door remediation surface) was condition-walks surfacing integration gaps, not new mechanism. Finishing stretch: A must-fix → G admin-config → D UI polish + native → F staging. Plus deferred conversation improvements (B) and the productionization items (incl. the Yield/Quality module + predictive-by-module roadmap).
- **Shift-model keystone (C1 engine) committed + verified** — calendar-aware placement, OT meaningful (seed loaded so lines run realistically full; line-down → reroute-tomorrow vs OT-extend-today confirmed), day-axis = working window. Week view + date nav built + web-verified.
- **LANDED SINCE (this work cycle), all committed + merged:**
  - **Line-down condition** — complete (per-resource `resource_downtime`, displacing closure, reroute/OT what-if, stale-cache + banner + stranded-vs-at-risk fixes).
  - **Operator-performance condition** — complete: operator as a first-class lateness root (counterfactual attribution, migration 0018 `binding_operator_id`), operator labor folded into the cost objective (wi-12), the **faster-operator remediation lever** (Part B), and the Copilot narrating the operator HOW (mechanism, not just who).
  - **Infeasibility scoring (wi-13)** — window-overflow infeasibility folded into firm-lateness via a fixed sentinel (`infeasibleFirmOps × 100_000`); honest KPIs (`firmLateHours` stays real, `infeasibleFirmOps` count added). The scoring analog of the stranded model.
  - **Non-options / selectability + per-order verdict** — infeasible plans are non-options (demoted to a stat-less line, never a selectable choice); honest-unremediable via the explicit `unremediable` field; per-order verdict (`targetOutcome`) fixes the plant-wide confound by construction (has-options / can't-be-on-time / can't-run).
  - **Two-door at-risk remediation** — "See options" (direct costed option-set card, bounded recommendation) + "Evaluate options" (Copilot, anchored to the deterministic result, open exploration). Closes the direct-option-set backlog item (B). (The separate line-down "See options" UI bug (A) is closed by the condition-card wiring + popup migration — a different surface.)
  - **Cockpit / scorecard KPI overhaul** — historical OEE on the cockpit (`measured_historical`, Option-A seed: ~79% plant, availability-led, Press A weak) + per-version OEE on the scorecard (A·P·Q breakdown bars); **fixed the executed-ops dilution bug** (per-version OEE/throughput/adherence were summing over unexecuted ops → 34% Performance); canonical at-risk from the work-list status engine (cockpit/scorecard/baseline reconcile, was 15/6/0); reseeded historical cost near live (believable lift); curated KPI sets (cockpit: On-Time/Utilization/At-Risk/OEE; scorecard: On-Time/Adherence/OEE/Cost); On-Time labeling honest + consistent across both surfaces (NOT OTIF — the metric is on-time-vs-due only, no in-full check, and the demo isn't in-full; Schedule Adherence is the distinct metric). **Validated across BOTH plants (Saltillo + Ramos).**
  - **Copilot version-awareness (Tier 1, `64ffc8b`)** — `explain_lateness` follows the viewed draft (dry-run proven); Tier 2 deemed inert (other tools re-sequence from live inputs).
- **NEW: the Yield/Quality module is now captured as the #1 post-demo gap** (Future phases) + the prediction-ownership architecture cross-referenced — these were the most consequential items previously living only in conversation / the separate `PREDICTION-OWNERSHIP-BY-MODULE.md`.
- **Convention:** every deferred item — including production-only — is captured in this doc, not left in conversation memory.
- **Next (demo-prep, ~the remaining stretch):** A must-fix (per-option narration scoping — runtime re-check before closing; Copilot markdown rendering) → G admin-config #1/#2 → D UI polish + native verification pass (UI changed a lot this cycle) → **F staging** (the bulk: seed coherence, the **operator-beat tuning** — tight Press-A order + Ana ~0.30 for an `operator` root, NOT a global poke, per the dry-run finding; run-of-show; talk track; `recorded` fallback — the Groq flake in the dry-run is a live reminder; rehearsal). Quality/yield, the 902 Performance/KPI dashboard, and the predictive-by-module roadmap are post-demo (seeded/curated for the demo, not built).
- **Dev gotcha logged:** i18n JSON edits don't hot-reload (static imports loaded into i18next once at init) — **restart the dev server to see label changes**, or a correct edit looks like it "didn't apply."
